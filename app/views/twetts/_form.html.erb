<%= form_with(model: twett, local: true) do |form| %>
  <% if twett.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twett.errors.count, "error") %> prohibited this twett from being saved:</h2>

      <ul>
      <% twett.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :twett %>
    <%= form.text_area :twett %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
